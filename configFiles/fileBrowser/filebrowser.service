[Unit]
Description=File browser: %I
After=network.target

[Service]
User=pi
ExecStart=sudo /usr/local/bin/filebrowser -c /etc/filebrowser/%I -a 0.0.0.0 -p 8090 -r /mnt

[Install]
WantedBy=multi-user.target
